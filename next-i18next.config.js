/** @type {import('next-i18next').UserConfig} */
const config = {
  i18n: {
    defaultLocale: 'en',
    locales: ['en', 'de', 'it', 'sv'],
  },
  localePath: './public/locales',
};

export default config;
