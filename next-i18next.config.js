/** @type {import('next-i18next').UserConfig} */
const config = {
  i18n: {
    defaultLocale: 'en',
    locales: ['en', 'de', 'it'],
  },
  localePath: './public/locales',
};

export default config;
